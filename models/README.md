# Vosk model

The repository keeps the old fallback model:

`models/vosk-model-small-ru-0.22`

The default streaming Vosk 0.54 model is downloaded and checksum-verified by
the installer under `.cache/liverse/models/`.
