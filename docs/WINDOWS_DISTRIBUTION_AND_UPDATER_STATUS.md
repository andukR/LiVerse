# Windows-дистрибутив и обновлятор LiVerse

Состояние зафиксировано 25 августа 2026 года перед началом работы над
обновлением готового Windows-дистрибутива.

## Что уже работает

- `make windows-vm-test` проверяет исходный код в Linux и Windows VM.
- `make windows-vm-engine` дополнительно собирает папку PyInstaller `onedir` с
  `LiVerse.exe`, `LiVerseEngine.exe` и необходимыми библиотеками и данными.
- `make windows-vm-installer` собирает установщик
  `LiVerse-Setup-<version>.exe`, выполняет чистую установку, проверяет GUI,
  движок и runtime-ресурсы, удаляет тестовую установку и проверяет сохранность
  пользовательских данных.
- Настройки и журналы Windows-версии находятся в `%LOCALAPPDATA%\LiVerse`, а
  не в каталоге программы. Переустановка программы поэтому не должна удалять
  настройки пользователя.
- Установщик LiVerse 1.1.0 был вручную проверен в Windows VM: ярлык, настройка,
  распознавание и показ стихов в HoLyrics работали.

## Проверенная сборка 1.1.0

- Путь в VM: `C:\Build\LiVerse\dist\installer\LiVerse-Setup-1.1.0.exe`
- Размер: `174625899` байт.
- SHA-256: `42ef41a1852d4101a83feb5ff176a7c8ea86c02cef4c51923b28053ee62af848`.
- Сборка не подписана цифровой подписью и предназначена для закрытого
  бета-тестирования.

## Что означает аналог `./gradlew installDebug`

Ближайшая существующая команда — `make windows-vm-installer`. Она полностью
собирает и проверяет установщик, но после автоматической проверки удаляет
тестовую установку. Отдельной команды, которая оставляет новую сборку
установленной и запускает её в видимом рабочем столе Windows, пока нет.

## Два разных механизма обновления

1. Установка LiVerse из исходников использует существующий Git-обновлятор. Он
   выполняет `git fetch`, обновляет рабочую копию и Python-зависимости. Этот
   механизм нужен разработческой/Linux-установке.
2. Готовый Windows-дистрибутив не должен требовать Git, Python, `pip` или
   виртуальное окружение. Для него нужен бинарный обновлятор: скачать новый
   `LiVerse-Setup-<version>.exe`, проверить файл и запустить установщик.

Эти механизмы нельзя смешивать. GUI должен выбирать бинарный обновлятор только
когда он запущен как упакованный Windows `.exe`.

## Реализованный контракт бинарного обновления

- Источник сведений: последний опубликованный выпуск GitHub Releases
  репозитория `andukR/LiVerse`.
- Номер версии: тег выпуска вида `v1.1.1`.
- Файл: ресурс выпуска с точным именем `LiVerse-Setup-1.1.1.exe`.
- Контрольная сумма: поле `digest` ресурса GitHub вида `sha256:<64 цифры>`.
- LiVerse сначала спрашивает разрешение пользователя, затем скачивает файл в
  `%LOCALAPPDATA%\LiVerse\updates`, сверяет размер и SHA-256 и только после
  успешной проверки запускает установщик.
- При отсутствии сети, выпуска, установщика или SHA-256 установленная версия
  продолжает работать без изменений.
- Обновлятор не обходит Windows SmartScreen и не изображает цифровую подпись.

Локальная реализация находится в `tools/release_updater.py`, а выбор между ней
и старым Git-обновлением — в `tools/liverse_gui.py`. Добавлены отдельные тесты
проверки версии, маршрута GUI, размера, SHA-256, атомарного скачивания и отказа
от повреждённого файла. После изменений полный набор содержит 245 тестов и
проходит на Linux.

`make windows-release VERSION=<номер>` является строгим вариантом сборки: он
требует ветку `main`, чистое рабочее дерево и совпадение указанного номера с
`version.py`. Это защищает официальный выпуск, не запрещая промежуточные
диагностические сборки командой `make windows-vm-installer`.

## Диагностическая проверка обновлятора в Windows

25 августа 2026 года после реализации обновлятора выполнен полный
`make windows-vm-installer` в офлайн Windows 10 VM:

- Linux: 245 тестов, все прошли;
- Windows: 245 тестов, все прошли;
- снимок: 74 файла, SHA-256-манифест проверен;
- PyInstaller `onedir`: GUI, движок, база, Sherpa 0.54 и аудиоустройства прошли
  проверки;
- Inno Setup: сборка, чистая установка, запуск и удаление прошли;
- пользовательские данные после удаления сохранились;
- созданы файл SHA-256 и JSON-отчёт выпуска.

Диагностический установщик:

- `C:\Build\LiVerse\dist\installer\LiVerse-Setup-1.1.0.exe`;
- размер `174634914` байт;
- SHA-256 `075969508ba0550ac8f9d769232e31cb4360903aeb13e5844ce14fa81d35a42c`.

Этот файл собран из `dirty=true` и не является новым выпуском. Он намеренно не
публикуется и не должен заменять уже переданную тестировщикам сборку 1.1.0.

## Состояние Git на момент фиксации

- Ветка: `main`.
- Коммит: `88e3b34` (`Merge Windows fixes into main`).
- Локальная ветка опережала `origin/main` на восемь коммитов.
- Рабочее дерево содержало незакоммиченные изменения Windows-сборки и других
  уже начатых функций. Поэтому публиковать новый официальный выпуск из этого
  состояния нельзя.
- На GitHub ещё не было опубликованного Release: запрос последнего выпуска
  возвращал HTTP 404.

## Оставшиеся этапы

1. Поднять номер версии и собрать новый установщик из чистого коммита.
2. Проверить установку новой версии поверх предыдущей с сохранением настроек.
3. Опубликовать GitHub Release с тегом версии и установщиком.
4. На установленной предыдущей версии проверить полный путь: обнаружение,
   вопрос пользователю, скачивание, SHA-256, запуск установщика и обновление.

## Проверка упакованного обновлятора

25 августа 2026 года безопасный этап проверки бинарного канала завершён:

- Linux: 255 тестов, все прошли;
- Windows 10 VM: 255 тестов, все прошли;
- сетевой интерфейс VM `vnet0` оставался отключённым;
- собраны реальные `LiVerse.exe` и `LiVerseEngine.exe`;
- прошли проверки GUI, трея, базы Библии, Sherpa 0.54 и аудиоустройств;
- `LiVerse.exe --check-packaged-update` завершился с кодом 0: упакованная
  Windows-версия выбрала бинарный канал и безопасно обработала отсутствие
  сети, ничего не скачивая и не устанавливая.

Диагностическая сборка была создана из `dirty=true`, потому что сама команда
проверки ещё не была закоммичена. Это не выпуск для распространения.
